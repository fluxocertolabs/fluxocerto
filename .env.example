# =============================================================================
# Application Configuration
# =============================================================================

# Base URL for the application (used by E2E tests)
# Default: http://localhost:5173
# Change this if you need to run on a different port locally
BASE_URL=http://localhost:5173

# =============================================================================
# Supabase Configuration
# =============================================================================
# For LOCAL development: Run `pnpm db:start` to start Supabase locally.
# For PRODUCTION: Get these values from your Supabase project dashboard:
#   Settings → API → Project URL and anon public key

# Supabase API URL
# Local: http://127.0.0.1:54321
# Production: https://your-project-id.supabase.co
VITE_SUPABASE_URL=http://127.0.0.1:54321

# Supabase Anonymous Key (safe to expose in frontend)
# Local: Use the demo key from `npx supabase status`
# Production: Get from Supabase dashboard
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Service Role Key for admin operations (E2E tests only - NEVER expose in frontend!)
# Get this from `npx supabase status` after starting Supabase locally
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# =============================================================================
# E2E Testing Configuration
# =============================================================================

# Inbucket URL for email testing (local Supabase includes Inbucket)
# Default: http://localhost:54324
INBUCKET_URL=http://localhost:54324

# Test user email for E2E tests
# Default: e2e-test@example.com
TEST_USER_EMAIL=e2e-test@example.com

# =============================================================================
# Local Development Auth Bypass (DEV MODE ONLY)
# =============================================================================
# These tokens allow skipping the login flow during local development.
# AI agents and developers can immediately access authenticated views.
#
# To generate tokens:
#   1. Start Supabase: pnpm db:start
#   2. Run: pnpm run gen:token
#   3. Copy the output tokens here
#
# IMPORTANT: These tokens only work in DEV mode (pnpm dev:app).
# They have no effect in production builds.

# Dev access token (JWT) - generated by gen:token script
# VITE_DEV_ACCESS_TOKEN=eyJhbGciOiJIUzI1NiIsInR5cCI6...

# Dev refresh token - generated by gen:token script
# VITE_DEV_REFRESH_TOKEN=abc123...
