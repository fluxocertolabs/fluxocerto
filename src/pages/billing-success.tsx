export function BillingSuccessPage() {
  // This route now redirects immediately to `/` and shows an overlay there.
  // Keeping this component as a harmless fallback for older builds.
  return null
}

